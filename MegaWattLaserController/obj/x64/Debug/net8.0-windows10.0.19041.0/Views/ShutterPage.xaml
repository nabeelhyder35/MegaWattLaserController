<Page x:ConnectionId='1' x:Class="LaserControllerApp.Views.ShutterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              >

    <Grid Margin="20">
        <StackPanel Spacing="15">
            <TextBlock Text="SHUTTER CONTROL" 
                       FontSize="16" 
                       FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button x:ConnectionId='4' x:Name="OpenShutterButton"
                        Content="Open Shutter" 
                                                  
                        Width="120"/>

                <Button x:ConnectionId='5' x:Name="CloseShutterButton"
                        Content="Close Shutter" 
                                                  
                        Width="120"/>
            </StackPanel>

            <InfoBar x:ConnectionId='2' x:Name="StatusInfoBar" 
                     Title="Shutter Status"
                     IsOpen="False"
                     HorizontalAlignment="Stretch"/>

            <TextBlock x:ConnectionId='3' x:Name="StatusTextBlock" 
                       Text="Shutter status unknown"
                       FontStyle="Italic"
                       Foreground="{ThemeResource SystemBaseMediumHighColor}"/>

            <TextBlock Text="Note: Shutter is external to laser driver" 
                       Foreground="Gray" 
                       FontSize="12"
                       FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</Page>

