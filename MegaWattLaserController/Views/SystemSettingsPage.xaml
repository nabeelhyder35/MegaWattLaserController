<Page x:Class="LaserControllerApp.Views.SystemSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">

    <Page.Resources>
        <Style x:Key="SettingsButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="80"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsGridStyle" TargetType="Grid">
            <Setter Property="Margin" Value="20"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <ScrollViewer>
            <StackPanel>
                <!-- Header -->
                <Border Background="{StaticResource SystemAccentColorDark2}" 
                        CornerRadius="8" 
                        Padding="20">
                    <StackPanel>
                        <TextBlock Text="SYSTEM SETTINGS" 
                                   FontSize="18" 
                                   FontWeight="Bold"
                                   Foreground="White"/>

                        <TextBlock Text="Configure laser system parameters and settings"
                                   FontSize="12"
                                   Foreground="White"
                                   Opacity="0.8"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Settings Grid -->
                <Grid Style="{StaticResource SettingsGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 1 -->
                    <Button x:Name="PulseSettingsButton" 
                            Content="Pulse Settings" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="0" Grid.Column="0"
                            Click="PulseSettingsButton_Click"
                            ToolTipService.ToolTip="Configure pulse parameters"/>

                    <Button x:Name="VoltageSettingsButton" 
                            Content="Voltage Control" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="0" Grid.Column="1"
                            Click="VoltageSettingsButton_Click"
                            ToolTipService.ToolTip="Set voltage parameters"/>

                    <Button x:Name="DelaySettingsButton" 
                            Content="Delay Settings" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="0" Grid.Column="2"
                            Click="DelaySettingsButton_Click"
                            ToolTipService.ToolTip="Configure timing delays"/>

                    <Button x:Name="InterlockSettingsButton" 
                            Content="Interlock Config" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="0" Grid.Column="3"
                            Click="InterlockSettingsButton_Click"
                            ToolTipService.ToolTip="Configure interlock settings"/>

                    <!-- Row 2 -->
                    <Button x:Name="ShutterSettingsButton" 
                            Content="Shutter Control" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="1" Grid.Column="0"
                            Click="ShutterSettingsButton_Click"
                            ToolTipService.ToolTip="Control laser shutter"/>

                    <Button x:Name="SoftStartSettingsButton" 
                            Content="Soft Start" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="1" Grid.Column="1"
                            Click="SoftStartSettingsButton_Click"
                            ToolTipService.ToolTip="Configure soft start parameters"/>

                    <Button x:Name="WaveformSettingsButton" 
                            Content="Waveform" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="1" Grid.Column="2"
                            Click="WaveformSettingsButton_Click"
                            ToolTipService.ToolTip="Waveform display settings"/>

                    <Button x:Name="SystemInfoButton" 
                            Content="System Info" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="1" Grid.Column="3"
                            Click="SystemInfoButton_Click"
                            ToolTipService.ToolTip="View system information"/>

                    <!-- Row 3 -->
                    <Button x:Name="UserSettingsButton" 
                            Content="User Settings" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="2" Grid.Column="0"
                            Click="UserSettingsButton_Click"
                            ToolTipService.ToolTip="User configuration settings"/>

                    <Button x:Name="FactorySettingsButton" 
                            Content="Factory Settings" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="2" Grid.Column="1"
                            Click="FactorySettingsButton_Click"
                            ToolTipService.ToolTip="Factory-level configuration (requires password)"/>

                    <Button x:Name="PasswordSettingsButton" 
                            Content="Password" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="2" Grid.Column="2"
                            Click="PasswordSettingsButton_Click"
                            ToolTipService.ToolTip="Change password settings"/>

                    <Button x:Name="ConfigSaveButton" 
                            Content="Save/Restore" 
                            Style="{StaticResource SettingsButtonStyle}"
                            Grid.Row="2" Grid.Column="3"
                            Click="ConfigSaveButton_Click"
                            ToolTipService.ToolTip="Save and restore configurations"/>
                </Grid>

                <!-- Current Settings Display -->
                <Border Background="{StaticResource SystemAltMediumColorBrush}" 
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,25,0,0">
                    <StackPanel>
                        <TextBlock Text="CURRENT SYSTEM STATUS" 
                                   FontSize="16" 
                                   FontWeight="Bold"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Connection Status:" 
                                       Grid.Row="0" Grid.Column="0"
                                       FontWeight="SemiBold"/>
                            <TextBlock x:Name="ConnectionStatusText" 
                                       Text="Disconnected" 
                                       Grid.Row="0" Grid.Column="1"
                                       Foreground="Red"/>

                            <TextBlock Text="Laser State:" 
                                       Grid.Row="1" Grid.Column="0"
                                       FontWeight="SemiBold"/>
                            <TextBlock x:Name="LaserStateText" 
                                       Text="Idle" 
                                       Grid.Row="1" Grid.Column="1"/>

                            <TextBlock Text="Active Settings Profile:" 
                                       Grid.Row="2" Grid.Column="0"
                                       FontWeight="SemiBold"/>
                            <TextBlock x:Name="SettingsProfileText" 
                                       Text="Default" 
                                       Grid.Row="2" Grid.Column="1"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Quick Actions -->
                <StackPanel Orientation="Horizontal" Margin="0,25,0,0" HorizontalAlignment="Center">
                    <Button x:Name="SaveSettingsButton" 
                            Content="Save Current Settings" 
                            Click="SaveSettingsButton_Click"
                            Margin="5"/>

                    <Button x:Name="LoadSettingsButton" 
                            Content="Load Settings" 
                            Click="LoadSettingsButton_Click"
                            Margin="5"/>

                    <Button x:Name="ResetSettingsButton" 
                            Content="Reset to Default" 
                            Click="ResetSettingsButton_Click"
                            Margin="5"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>