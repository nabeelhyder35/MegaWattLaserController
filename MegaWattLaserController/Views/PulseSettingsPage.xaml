<Page x:Class="LaserControllerApp.Views.PulseSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Unloaded="Page_Unloaded">

    <Grid Margin="20">
        <ScrollViewer>
            <StackPanel Spacing="20">
                <!-- Operation Mode -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="20">
                    <StackPanel Spacing="15">
                        <TextBlock Text="OPERATION MODE" 
                                   FontSize="16" 
                                   FontWeight="Bold"/>

                        <ComboBox x:Name="ModeComboBox" 
                                  Header="Select Mode:"
                                  SelectionChanged="ModeComboBox_SelectionChanged">
                            <ComboBoxItem Content="Continuous" Tag="CONTINUOUS"/>
                            <ComboBoxItem Content="Burst" Tag="BURST"/>
                        </ComboBox>

                        <!-- Burst Settings (Visible only in Burst mode) -->
                        <StackPanel x:Name="BurstSettingsPanel" 
                                    Spacing="10"
                                    Visibility="Collapsed">
                            <TextBox x:Name="BurstCountTextBox" 
                                     Header="Burst Count:"
                                     PlaceholderText="Enter number of pulses (1-1000)"
                                     InputScope="Number"/>

                            <TextBlock Text="Internal/External Trigger:"
                                       FontWeight="SemiBold"/>

                            <StackPanel Orientation="Horizontal" Spacing="20">
                                <RadioButton x:Name="InternalTriggerRadio" 
                                             Content="Internal"
                                             GroupName="TriggerGroup"
                                             IsChecked="True"/>
                                <RadioButton x:Name="ExternalTriggerRadio" 
                                             Content="External"
                                             GroupName="TriggerGroup"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Pulse Parameters -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="20">
                    <StackPanel Spacing="15">
                        <TextBlock Text="PULSE PARAMETERS" 
                                   FontSize="16" 
                                   FontWeight="Bold"/>

                        <!-- Repetition Rate -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Repetition Rate:"
                                       Grid.Row="0" Grid.Column="0"
                                       VerticalAlignment="Center"/>

                            <Slider x:Name="RepRateSlider"
                                    Grid.Row="1" Grid.Column="0"
                                    Minimum="1" Maximum="2000" Value="10"
                                    ValueChanged="RepRateSlider_ValueChanged"/>

                            <TextBox x:Name="RepRateTextBox"
                                     Grid.Row="1" Grid.Column="1"
                                     Margin="10,0,0,0"
                                     Text="{Binding Value, ElementName=RepRateSlider, Mode=TwoWay}"
                                     InputScope="Number"/>

                            <TextBlock Text="Hz"
                                       Grid.Row="1" Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       Margin="0,0,25,0"/>

                            <TextBlock x:Name="FrequencyValueText"
                                       Grid.Row="0" Grid.Column="1"
                                       Text="10 Hz"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Pulse Width -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Pulse Width:"
                                       Grid.Row="0" Grid.Column="0"
                                       VerticalAlignment="Center"/>

                            <Slider x:Name="PulseWidthSlider"
                                    Grid.Row="1" Grid.Column="0"
                                    Minimum="25" Maximum="25000" Value="100"
                                    ValueChanged="PulseWidthSlider_ValueChanged"/>

                            <TextBox x:Name="PulseWidthTextBox"
                                     Grid.Row="1" Grid.Column="1"
                                     Margin="10,0,0,0"
                                     Text="{Binding Value, ElementName=PulseWidthSlider, Mode=TwoWay}"
                                     InputScope="Number"/>

                            <TextBlock Text="μs"
                                       Grid.Row="1" Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       Margin="0,0,25,0"/>

                            <TextBlock x:Name="PulseWidthValueText"
                                       Grid.Row="0" Grid.Column="1"
                                       Text="100 μs"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <Button x:Name="ApplyPulseSettingsButton"
                                Content="Apply Settings"
                                Click="ApplyPulseSettingsButton_Click"
                                HorizontalAlignment="Right"/>
                    </StackPanel>
                </Border>

                <!-- Response Display -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="20">
                    <StackPanel Spacing="15">
                        <TextBlock Text="RESPONSE" 
                                   FontSize="16" 
                                   FontWeight="Bold"/>

                        <InfoBar x:Name="ResponseInfoBar" 
                                 Title="FPGA Response"
                                 IsOpen="False"
                                 HorizontalAlignment="Stretch"/>

                        <TextBlock x:Name="ResponseTextBlock" 
                                   TextWrapping="Wrap"
                                   FontFamily="Consolas"
                                   Foreground="{ThemeResource SystemBaseMediumHighColor}"
                                   MinHeight="60"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>