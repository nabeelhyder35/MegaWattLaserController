<Window
    x:Class="LaserControllerApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaserControllerApp"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="Laser Controller">

    <Grid x:Name="RootGrid" DataContext="{x:Bind ViewModel, Mode=OneTime}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <StackPanel Orientation="Horizontal" Margin="8" Spacing="8">
            <ComboBox Width="120"
                      ItemsSource="{x:Bind ViewModel.AvailablePorts, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedPort, Mode=TwoWay}"
                      PlaceholderText="Select Port"/>
            <ComboBox Width="120"
                      ItemsSource="{x:Bind ViewModel.BaudRates, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedBaudRate, Mode=TwoWay}"/>
            <Button Content="Refresh" Command="{x:Bind ViewModel.RefreshPortsCommand}"/>
            <Button Content="Connect" Command="{x:Bind ViewModel.ConnectCommand}"/>
            <Button Content="Disconnect" Command="{x:Bind ViewModel.DisconnectCommand}"/>
            <TextBlock Text="{x:Bind ViewModel.CurrentState, Mode=OneWay}" 
                       FontWeight="Bold" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <!-- NavigationView -->
        <muxc:NavigationView x:Name="NavigationViewControl"
                             Grid.Row="1"
                             PaneDisplayMode="Left"
                             IsBackButtonVisible="Collapsed"
                             IsSettingsVisible="True"                          
                             SelectionChanged="NavigationViewControl_SelectionChanged">

            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Content="Laser Control" Tag="PulseSettingsPage"/>
                <muxc:NavigationViewItem Content="Shutter Control" Tag="ShutterPage"/>
                <muxc:NavigationViewItem Content="Energy Monitor" Tag="EnergyMonitorPage"/>
                <muxc:NavigationViewItem Content="Voltage Monitor" Tag="VoltagePage"/>              
                <muxc:NavigationViewItem Content="Waveform Display" Tag="WaveformPage"/>
                <muxc:NavigationViewItem Content="Interlock Status" Tag="InterlockStatusPage"/>
                <muxc:NavigationViewItem Content="Custom Commands" Tag="CustomPage"/>
            </muxc:NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </muxc:NavigationView>

        <!-- Error Bar -->
        <Border Grid.Row="2" Background="DarkRed" Padding="8"
                Visibility="{x:Bind ViewModel.HasError, Mode=OneWay}">
            <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="White" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>