<Window
    x:Class="LaserControllerApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:LaserControllerApp"
    mc:Ignorable="d"
    Title="Laser Controller App">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Connection Status Bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Padding="10">
            <Ellipse x:Name="ConnectionStatusEllipse" Width="12" Height="12" Fill="Red" Margin="0,0,5,0"/>
            <TextBlock x:Name="StatusTextBlock" Text="Disconnected" VerticalAlignment="Center" Margin="0,0,10,0"/>

            <ComboBox x:Name="PortComboBox" Width="150" Margin="0,0,5,0" 
                     Header="Port" PlaceholderText="Select port"/>
            <ComboBox x:Name="BaudRateComboBox" Width="100" Margin="0,0,5,0" 
                     Header="Baud Rate" SelectedIndex="0">
                <ComboBoxItem>9600</ComboBoxItem>
                <ComboBoxItem>19200</ComboBoxItem>
                <ComboBoxItem>38400</ComboBoxItem>
                <ComboBoxItem>57600</ComboBoxItem>
                <ComboBoxItem>115200</ComboBoxItem>
            </ComboBox>
            <Button x:Name="RefreshPortsButton" Content="Refresh" Click="RefreshPorts_Click" Margin="0,0,5,0"/>
            <Button x:Name="ConnectButton" Content="Connect" Click="Connect_Click" Margin="0,0,5,0"/>
            <Button x:Name="DisconnectButton" Content="Disconnect" Click="Disconnect_Click" IsEnabled="False"/>
        </StackPanel>

        <!-- Simple content area -->
        <Frame x:Name="ContentFrame" Grid.Row="1"/>
    </Grid>
</Window>